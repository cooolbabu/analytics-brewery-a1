import { Button } from "library-name"; // Replace 'library-name' with the actual library name

function ThemeToggle() {
  return <button className="btn btn-primary btn-sm">TG</button>;
}
export default ThemeToggle;
